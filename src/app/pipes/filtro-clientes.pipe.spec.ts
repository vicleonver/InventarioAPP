import { FiltroClientesPipe } from './filtro-clientes.pipe';

describe('FiltroClientesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroClientesPipe();
    expect(pipe).toBeTruthy();
  });
});
